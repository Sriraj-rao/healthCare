import { HeaderFilterPipe } from './header-filter.pipe';

describe('HeaderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
