import { InvestigationTypePipe } from './investigation-type.pipe';

describe('InvestigationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InvestigationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
