import { ProofOfLossPipe } from './proof-of-loss.pipe';

describe('ProofOfLossPipe', () => {
  it('create an instance', () => {
    const pipe = new ProofOfLossPipe();
    expect(pipe).toBeTruthy();
  });
});
